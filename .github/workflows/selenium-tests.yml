name: UI Testing Execution

on:
  push:
    branches: [ "HeilynPracticeKoel", "main" ]
  workflow_dispatch: 

jobs:
  ui-automation:
    runs-on: ubuntu-latest

    # Levantamos el Grid dentro de GitHub para evitar el ConnectTimeoutException
    services:
      selenium-grid:
        image: selenium/standalone-chrome:latest
        ports:
          - 4444:4444

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        ref: HeilynPracticeKoel

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Run Selenium Tests
      run: |
        # Usamos 'gradle' directamente ya que no usas el wrapper
        # -DgridUrl sobreescribe tu IP local (10.0.0.43) por la del Grid de GitHub
        gradle test -DCHROME_HEADLESS=true -DgridUrl=http://localhost:4444/wd/hub

    - name: Upload Test Reports
      if: always() 
      uses: actions/upload-artifact@v4
      with:
        name: ui-test-results
        path: "**/build/reports/tests/test/"
