name: UI Testing Execution

on:
  push:
    branches: [ "HeilynPracticeKoel" ]
  workflow_dispatch: 

jobs:
  ui-automation:
    runs-on: ubuntu-latest

    # Permisos vitales para que GitHub pueda crear la rama del reporte
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: HeilynPracticeKoel

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Debug and Run
        run: |
          echo "--- COMPROBANDO ARCHIVOS ---"
          ls -la
          
          if [ ! -f "gradlew" ]; then
            echo "Gradlew no encontrado, intentando generarlo..."
            gradle wrapper
          fi
          
          chmod +x gradlew
          # Ejecución de tus 8 tests en modo headless para el servidor
          ./gradlew test -Dheadless=true --stacktrace

      - name: Upload Test Reports (JUnit XML)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ui-test-results
          path: |
            **/build/reports/tests/test/
            **/build/test-results/test/

      - name: Load Allure history
        uses: actions/checkout@v4
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
          allure_results: build/allure-results
          gh_pages: gh-pages
          allure_report: allure-report
          allure_history: allure-history
          keep_reports: 20

      - name: Create history folder if missing
        if: always()
        run: mkdir -p allure-history # Solución al error de git push

      - name: Deploy to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-history
          publish_branch: gh-pages
          force_orphan: true
